/** Mirrors active ng-crm segment-editor FilterType values. */
export enum SegmentFilterType {
  BOOLEAN = 'BOOLEAN',
  STRING = 'STRING',
  LIST = 'LIST',
  INTEGER = 'INTEGER',
  DECIMAL = 'DECIMAL',
  MONEY = 'MONEY',
  DATE = 'DATE',
  DATE_TIME = 'DATE_TIME',
  INTERVAL = 'INTERVAL',
}

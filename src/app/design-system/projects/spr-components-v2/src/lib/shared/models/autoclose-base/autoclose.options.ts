import { AppearanceAnimationType, AppearanceAnimationTypes } from '../../animations';

const DEFAULT_AUTOCLOSE_DELAY: number = 5;
const DEFAULT_ANIMATION: AppearanceAnimationType = AppearanceAnimationTypes.FADE;

export { DEFAULT_ANIMATION, DEFAULT_AUTOCLOSE_DELAY };
